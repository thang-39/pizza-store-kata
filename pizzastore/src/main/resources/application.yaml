
spring:
  jpa:
#    database-platform: org.hibernate.dialect.PostgreSQL94Dialect
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    # PostgresSQL
    url: jdbc:postgresql://localhost:5432/pizzastore
    username: postgres
    password: 123